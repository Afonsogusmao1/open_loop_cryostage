# Figure SX. Cryostage dry-cooling response with first-order model overlay

Suggested caption:

**Figure SX. Dry-cooling characterization of the cryostage plate-temperature response.** Calibrated plate-temperature traces recorded during dry cooling to fixed reference setpoints of -5, -10, -15, and -20 degC, with three independent runs per setpoint. The dark-blue dashed curves show the corresponding first-order plate-temperature response model used in the open-loop trajectory-design workflow, evaluated from the median measured plate temperature at the detected cooling start for each setpoint.

Suggested in-text mention:

The dry-cooling traces and corresponding first-order model responses used for this fit are shown in Figure SX.
