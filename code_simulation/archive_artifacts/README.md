# Archived Artifacts

This folder stores zipped or otherwise bundled artifacts that should stay out of the active workflow paths.

Use `code_simulation/results/` and `code_simulation/docs/active/` for active work. Keep this folder read-only unless you are explicitly archiving new non-code artifacts.
